/** Items per Discover browse request (initial SSR + each “load more”). */
export const DISCOVER_BROWSE_PAGE_SIZE = 20;

/** Max items a single browse API call may return. */
export const DISCOVER_BROWSE_MAX_PAGE = 40;
